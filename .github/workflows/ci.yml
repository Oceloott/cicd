name: "CI"

on: 
    push:
        paths: ~

jobs:
    hello:
        runs-on: "ubuntu-latest"
        name: "Hello"
        steps:
            - name: "Hello world"
              run: echo "Hello, world!"
            - name: "multiple  world"
              run: |
                echo "Say"
                echo "Hello"
            - name: "list files"
              run: ls